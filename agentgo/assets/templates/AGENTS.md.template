# 项目执行规则

## 输入与输出

主要输入：

{{INPUT_SOURCES}}

允许输出：

{{OUTPUT_TARGETS}}

## 来源优先级

1. 用户当前明确指令与已确认约束。
2. 可直接核验的原始数据和系统状态。
3. 工作区内最新、适用的项目文件。
4. 历史记录与合理推断；使用时必须标明依据和不确定性。

来源冲突时不静默取舍：指出冲突、优先采用更直接且更新的证据，必要时请用户决定。

## 固定闭环

1. 读取：确认目标、输入、现状、权限和成功标准。
2. 计划：选择最小改动路径，标出需确认的写入或高风险动作。
3. 执行：只操作任务范围内的对象，保留已有内容和用户改动。
4. 验证：检查实际结果，而不是只看命令是否成功。
5. 汇报：分别说明已完成、未完成、证据、假设和下一步。

## 幂等与失败恢复

- 执行前先查现状；重复运行不得重复创建、重复发送或覆盖有效结果。
- 可分步操作时记录已完成阶段，失败后从最近的安全检查点继续。
- 失败时停止连锁写入，保留现场，报告原因、影响范围和可恢复方案；不得靠反复重试掩盖根因。

## 工具与权限

{{PERMISSION_BOUNDARIES}}

默认最小权限、先读后写。写入、删除、权限变更和对外发送必须先确认对象、范围与后果。安装或连通工具只是前置条件，不能作为业务完成证明。

## 验收

{{VERIFICATION_RULES}}

验收结论必须能追溯到实际输出或系统状态；无法核验的内容标为推断或缺失。
