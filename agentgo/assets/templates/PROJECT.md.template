# 项目说明

本文件是项目目标、数据流、文件职责和总体状态的唯一事实来源。能力三分组只在 `README.md` 维护，执行、权限和验收规则只在 `AGENTS.md` 维护。

## 目标与用户

本项目服务于：

{{PRIMARY_USERS}}

目标是把已授权输入转化为可核验输出，并让项目边界和责任清晰可见。

## 数据流

输入来源：

{{INPUT_SOURCES}}

处理原则：读取并核验来源 → 按 `AGENTS.md` 执行 → 验证结果与权限边界 → 写入或交付。

输出目标：

{{OUTPUT_TARGETS}}

## 文件职责

- `SOUL.md`：放在独立档案根目录，只定义长期人格、服务风格和最高底线。
- `AGENTS.md`：放在工作区，是执行流程、权限边界和验收规则的唯一事实来源。
- `README.md`：放在工作区，是能力三分组和用户入口的唯一事实来源。
- `PROJECT.md`：放在工作区，是项目目标、数据流、文件职责和总体状态的唯一事实来源。

## 总体状态

在本节按实际项目维护目标进展、数据流是否就绪、交付是否验收和当前阻塞。具体能力属于“自动运行”“按需可用”还是“尚未启用”，只在 `README.md` 更新。

工具存在、授权曾成功或流程跑过一次，都不能单独证明业务持续可用。

## 授权状态变更

已获持续授权且目标、范围、频率固定的自动任务可直接执行。新增任务，改变范围、频率或接收人，超出授权边界，或涉及高风险写入、删除、权限变更时，必须先确认，并先做可回退的小范围验证。具体权限和验收要求只在 `AGENTS.md` 维护。
