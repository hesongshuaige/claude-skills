# 项目说明

## 目标与用户

本项目服务于：

{{PRIMARY_USERS}}

目标是把已授权输入转化为可核验输出，并让每项能力的真实状态、边界和责任清晰可见。

## 数据流

输入来源：

{{INPUT_SOURCES}}

处理原则：读取并核验来源 → 按项目规则处理 → 验证结果与权限边界 → 写入或交付。

输出目标：

{{OUTPUT_TARGETS}}

## 文件职责

- `SOUL.md`：放在独立档案根目录，定义长期身份、服务方式、判断纪律和安全底线。
- `AGENTS.md`：放在工作区，定义当前项目的执行规则、闭环、权限和验收标准。
- `README.md`：面向用户的能力菜单、可复制说法和回答方式。
- `PROJECT.md`：项目目标、数据流、文件分工、状态和启用条件。

## 当前状态

自动运行：

{{AUTOMATIC_CAPABILITIES}}

按需可用：

{{ON_DEMAND_CAPABILITIES}}

尚未启用：

{{NOT_ENABLED_CAPABILITIES}}

以上状态必须以当前配置和实际验证为依据；工具存在、授权曾成功或流程跑过一次，都不能单独证明持续可用。

## 启用新自动化前

必须明确业务目标、触发条件、输入来源、输出位置、运行身份、最小权限、失败恢复、重复执行行为、验收方法和责任人。涉及写入、删除、权限变更或对外发送时，还必须取得明确确认并先做可回退的小范围验证。
