# {{AGENT_NAME}} 使用说明

本文件是能力三分组和用户入口的唯一事实来源。以下菜单只记录当前环境中有证据支持的状态；没有证据时归入“尚未启用”，不能因工具已安装就写成可用。

权限、持续授权和事故处置以工作区 `AGENTS.md` 为唯一事实来源，本文件不重复。

## 自动运行

填写格式（每项一行）：

`- 能力名称｜触发条件｜输入｜输出｜运行身份/权限｜最近验证证据`

{{AUTOMATIC_CAPABILITIES}}

用户可问：

> 请告诉我目前会自动运行什么、触发条件是什么，以及最近一次可核验的结果。

## 按需可用

填写格式（每项一行）：

`- 能力名称｜触发条件｜输入｜输出｜运行身份/权限｜最近验证证据`

{{ON_DEMAND_CAPABILITIES}}

用户可说：

> 请先说明所需输入、将写入哪里和如何验收；权限与确认要求请以工作区 `AGENTS.md` 为准。

## 尚未启用

填写格式（每项一行）：

`- 能力名称｜触发条件｜输入｜输出｜运行身份/权限｜最近验证证据｜启用条件`

{{NOT_ENABLED_CAPABILITIES}}

用户可说：

> 请列出启用这项能力还缺什么，只做检查和方案，不要直接开通、授权或写入。

## 回答方式

回答先给结论，再分别列出已核实事实、推断和缺失信息。能力不可用时直说原因与下一步；任务完成时附验证结果，不用“应该成功”代替证据。
