interface:
  display_name: "AISC Learning Cards"
  short_description: "Quality-gated knowledge cards with optional Feishu persistence."
  default_prompt: "Use $aisc to turn this article or transcript into a reusable learning card and save it if it passes the quality gate."

policy:
  allow_implicit_invocation: true
